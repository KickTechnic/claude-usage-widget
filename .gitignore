# Dependencies
node_modules/

# Build output (electron-builder outputs to dist/ by default, covers NSIS installer, portable .exe, mac .dmg, linux AppImage)
dist/
out/

# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local
.env.*.local

# Electron
*.electron/

# Cache
.cache/
.temp/

# Local dev scratch files
msg.txt
RELEASE_NOTES_DRAFT.md
GITHUB_RELEASE_DESCRIPTION.txt
test-version.js

# Agent knowledge layer — TRACKED in this fork (upstream ignores both CLAUDE.md
# and .claude/). The lessons and the queue are worth nothing if they vanish on a
# fresh clone, and this fork is not sent upstream, so they are version-controlled
# here. Only the machine-local settings file stays ignored.
.claude/settings.local.json

# Code signing credentials (NEVER COMMIT THESE)
developer_id.key
developer_id.csr
developer_id.p12
developerID_application.cer

# Temp commit message files
commit_msg.txt
